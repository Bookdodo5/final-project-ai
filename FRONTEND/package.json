{
  "name": "frontend",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "node server.js",
    "build": "mkdir -p public && cp -r *.* public/ 2>/dev/null || :",
    "start": "node server.js"
  },
  "dependencies": {
    "express": "^5.1.0"
  }
}
